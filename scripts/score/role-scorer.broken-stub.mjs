#!/usr/bin/env node
// Deliberately broken stub for break-attempt 1: simulates a crashed engine
// (e.g. a bad deploy, a missing dependency). Exits nonzero, writes nothing.
console.error("simulated crash: cannot read upstream evidence feed");
process.exit(1);
